#version 410 core

// Depth value automatically written to gl_FragDepth
// This fragment shader is just for completeness, no output is actually needed
void main()
{
    // OpenGL automatically writes depth value to depth buffer
    // In shadow map rendering, we only care about depth information
}
